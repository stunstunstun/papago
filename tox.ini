[tox]
envlist = py27,py36

[testenv]
passenv =
    HOME CLIENT_ID CLIENT_SECRET
deps=
    -r{toxinidir}/test-requirements.txt
commands = coverage erase
           coverage run {envbindir}/nosetests
           coverage report --include=*papagotrans* --omit=*test*





